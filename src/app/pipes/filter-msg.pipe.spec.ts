import { FilterMsgPipe } from './filter-msg.pipe';

describe('FilterMsgPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMsgPipe();
    expect(pipe).toBeTruthy();
  });
});
